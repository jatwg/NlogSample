{
    "Logging": {
      "LogLevel": {
        // Default logging level for all providers
        "Default": "Information",
        // Override logging level for Microsoft namespaces
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    },
    "Runner": {
      // Delay in milliseconds for the Runner's DoActionAsync method
      "DelayMs": 3000
    }
  }